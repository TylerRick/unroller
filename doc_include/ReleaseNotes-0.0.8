* Added option to inspect the values of arguments as well as "normal" locals
* Keeping track of the call stack so that we can tell the user what we are returning *from* when we hit a 'return' event
* Added an experimental :rails preset
